spring:
  application:
    name: intricdatacollector
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate

openapi:
  name: intric-data-collector
  title: Intric Data Collector
  version: '@project.version@'
